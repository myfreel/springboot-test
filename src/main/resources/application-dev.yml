server:
   port: 8888
spring:
   datasource:
     url: jdbc:mysql://127.0.0.1:3306/sproottest?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
     username: root
     password:
     driver-class-name: com.mysql.cj.jdbc.Driver


     #mybatis:
     #   mapper-locations: classpath:generator/*.xml


logging:
   level:
     com.wang.springbootstudey: debug
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true